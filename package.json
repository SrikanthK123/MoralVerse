{
    "name": "moralverse-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm run build-client && npm run build-server",
        "build-client": "cd client && npm install && npm run build",
        "build-server": "cd server && npm install",
        "start": "cd server && node server.js"
    }
}